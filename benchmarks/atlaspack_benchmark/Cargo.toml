[package]
name = "atlaspack_benchmark"
version = "0.1.0"
edition = { workspace = true }
description = "Synthetic project generator and harness for Atlaspack benchmarks"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
